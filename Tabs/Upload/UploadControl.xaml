<UserControl x:Class="Ark.UploadControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ark.Tabs.Upload"
             mc:Ignorable="d" 
             d:DesignHeight="163" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance local:UploadModel}"
             FontSize="14">
    <Grid Background="#FFFFFFFF" Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Content="Загрузить" HorizontalAlignment="Center" Margin="3,3,3,3" VerticalAlignment="Top" Height="30" Width="80" Grid.Row="1" Command="{Binding AddFilesToQueue}"/>
        <DataGrid ItemsControl.ItemsSource="{Binding Queue}" 
                  Margin="3,3,3,3" 
                  Grid.Row="0" 
                  CanUserAddRows="False"
                  CanUserSortColumns="False"
                  IsReadOnly="True"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Путь" Binding="{Binding Filepath}"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}"/>
                <DataGridTextColumn Header="Сообщение ошибки" Binding="{Binding Exception.Message}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
